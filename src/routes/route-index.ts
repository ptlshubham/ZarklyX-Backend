import { Application } from "express";

export default (app: Application) => {
    // Mobile APP API's routes
    // app.use("/user", require("./api-app/user/user-api"));
    // app.use("/appVersion", require(".api-app/app-version/app-version-api"));
    // app.use("/settings", require("./api-app/settings/settings-api"));

    // // Web App Apis Route Index
    // app.use("/employee", require("./api-webapp/employee/employee-api"));
    // app.use("/zones", require("./api-webapp/zones/zones-api"));
};

